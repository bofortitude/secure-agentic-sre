---
description: Create a new tmux session and indicate the OpenCode to use tmux.
---

Create a new tmux session named $1 . If there is duplicate name tmux session exists, ask user to terminate it or pick another name.
