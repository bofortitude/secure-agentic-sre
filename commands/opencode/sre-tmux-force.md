---
description: Force the OpenCode to execute commands in tmux session.
---

IMPORTANT: In this conversation, all bash commands MUST be executed WITHIN the $1 tmux session (using tmux send-keys or similar), not executed on the host system directly.

